[pytest]
markers =
    smoke
    e2e
    xdist_group(name): mark test to run in the same xdist worker as others with the same group (used internally for parallel by feature file)
bdd_features_base_dir = tests/features/
